---
if (!import.meta.env.DEV) {
    throw new Response(null, {status:404});
}
---
<h1>Weather Playground</h1>

<label>
  Hue shift
  <input type="range" min="-20" max="20" value="0"
    oninput="document.documentElement.style.setProperty('--hue-shift', `${this.value}deg`)"
  />
</label>